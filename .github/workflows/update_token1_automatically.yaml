name: Updating a GitHub key - REPOB_V0_TOKEN1

on:
  push:
    branches:
      - main
      
jobs:
  job0_update_REPOB_V0_TOKEN1:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Assign a new value to the secret
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh secret set REPOB_V0_TOKEN1
