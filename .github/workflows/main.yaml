name: Dispatch event

on:
  push:
    branches:
      - main

jobs:
  dispatch:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - run: REPOA_name=$(cat cb.txt | cut -d '|' -f 2)
      - run: $REPOA_name
