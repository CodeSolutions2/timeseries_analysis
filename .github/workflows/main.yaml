name: Dispatch event

on:
  push:
    branches:
      - main

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Get desired repo name from input_text -cut text at newline char and take 2nd column
        run: export repositoryA_name = $(cat cb.txt | cut -d '/n' -f 2)
        
